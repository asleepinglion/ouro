"use strict";

var SuperJS = require('../../../index');
SuperJS.Sanitize = require('superjs-sanitize');

/**
 * Sanitizations can be enabled for command options or parameters.
 * Many common sanitizations exist in the Sanitize base class, but you can
 * easily add custom sanitiizations here.
 *
 * @exports Sanitize
 * @namespace SuperJS.Validator
 * @extends SuperJS.Validator.Sanitize
 */

module.exports = SuperJS.Sanitize.extend({


});
