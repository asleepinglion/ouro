"use strict";

var SuperJS = require('../../../index');
SuperJS.Validate = require('superjs-validate');

/**
 * Validations can be enabled for command options or parameters.
 * Many common validations exist in the Validate base class, but you can
 * easily add custom validations here.
 *
 * @exports Validate
 * @namespace SuperJS.Validator
 * @extends SuperJS.Validator.Validate
 */

module.exports = SuperJS.Validate.extend({


});